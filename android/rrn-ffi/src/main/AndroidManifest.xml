<!-- Originally generated by uniffi-bindgen-react-native. The `package` attribute
     it emitted is an error under AGP 8 — `namespace` in build.gradle replaces it.
     Nothing else belongs here: the FFI declares no permissions or components. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
</manifest>
