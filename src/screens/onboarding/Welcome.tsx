import {PlaceholderScreen} from '../PlaceholderScreen';

/** Real content lands in T1.2.2 (Onboarding: create new wallet). */
export function Welcome() {
  return <PlaceholderScreen name="Welcome" />;
}
