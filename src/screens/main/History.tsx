import {PlaceholderScreen} from '../PlaceholderScreen';

/** Real content lands in T1.2.7 (Transaction history view with detail drilldown). */
export function History() {
  return <PlaceholderScreen name="History" />;
}
